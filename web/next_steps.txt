# Next Steps - TaskManager Web

> **Current State**: Web app is live in Staging with full Auth and CRUD connectivity. Build-time crashes are resolved, unit testing is established, structured logging is fully migrated (no `console.error` in source), and CI/CD gates deploys on test results.

## 1. Environment Configuration
- [x] Populate `web/.env.local` with real Firebase credentials.
- [x] Register the web app in the Firebase Console and enable App Check for the project.

## 2. Authentication Implementation
- [x] Create an `AuthProvider` using React Context to manage user sessions.
- [x] Implement the "Login with Google" logic in `src/lib/firebase.ts`.
- [x] Protect routes using a higher-order component or a middleware check.

## 3. Component Development
- [x] Build out the dashboard UI using the established Tailwind design system.
- [x] Create reusable components: `TaskCard`, `ProjectHeader`, `Sidebar`.
- [x] Ensure all components are responsive and support dark mode.

## 4. API Integration
- [x] Create a `src/lib/api.ts` client to handle requests to `/api/**`.
- [x] Use the `/shared` TypeScript interfaces to ensure end-to-end type safety.
- [x] Implement error handling and loading states for all data fetching.

## 5. CI/CD Activation
- [x] Push the codebase to GitHub.
- [x] Ensure the GitHub Actions secrets (`WIF_PROVIDER`, `WIF_SERVICE_ACCOUNT`) are configured.
- [x] Monitor the first deployment via the `.github/workflows/deploy.yml` pipeline.

## 6. Future Features
- [ ] Add drag-and-drop task reordering.
- [ ] Implement real-time task updates using Firestore listeners.
- [ ] Add comprehensive E2E tests using Playwright.
